"""Graph build + routing-engine client (handbook §3.4, §3.5).

In Phase A the pipeline stops at ``G_base``: there is no ``G_base → G″ → G′``
mutation (that is the Phase B forward-hook in :func:`rro.graph.build.build_graph`).
"""
